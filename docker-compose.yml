services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      DB_DIR: /app/Shrinker
    volumes:
      - shrinker-db:/app/Shrinker

volumes:
  shrinker-db: